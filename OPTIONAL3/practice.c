#include <stdlib.h>
#include <stdio.h>

int main()
{

	int x, y;		/* TO DO: the inputs you should take from console */
                                /*        (You may need more than just x and y).  */
	int out = 0;

	/* TO DO: Print a prompt requesting N integers, then read N integers */
	/*        into the variables declared above for you (x,y,...).       */
	/*        (N is defined for you in mp_spec.txt.)                     */   


	/* TO DO: Implement the function F(x,y,...) specified in mp_spec.txt. */


	/* TO DO: Print the input values and the function value to the console. */
	/*        Use the format "F(x,y,...) = out", where x and y are the      */
	/*        actual input values, and out is the value of F at (x,y,...).  */



	return 0;
}
